kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nvideo-rabbitmq-pvc
spec:
  storageClassName: nvideo-server-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi